<view class="counter">
  <button class="counter__btn" type="primary" size="mini" bind:tap="handleReduce">-</button>
  <view class="counter__text">{{ counter }}</view>
  <button class="counter__btn" type="primary" size="mini" bind:tap="handleAdd">+</button>
</view>
